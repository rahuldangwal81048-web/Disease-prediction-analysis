# Disease-prediction-analysis
Front-end-only educational symptom checker built with HTML, CSS and vanilla JavaScript. Uses a transparent weighted symptom-matching algorithm, shows a match score with runner-ups and red-flag warnings, and saves history in localStorage. No backend needed.
